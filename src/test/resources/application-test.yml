spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:mysql://db:3306/modern_bank?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: mincheol
    password: password123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: redis
      port: 6379

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true